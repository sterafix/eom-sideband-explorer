"""Streamlit page bodies for the EOM Sideband Explorer.

Each module exposes a ``render()`` function used as an ``st.Page`` entry
point from ``app.py``. These modules are the presentation layer only: they
read controls, call into :mod:`core` for the numbers, and draw the figures.
"""
