"""Physics core of the EOM Sideband Explorer.

Every module in this package is pure, Streamlit-free, and independently
tested (see ``tests/``). UI code (``app.py``, ``views/``) imports from here
and only handles sliders, layout, and plotting calls.
"""
