# Runtime dependencies — everything needed to *run* the app.
# Install with:  pip install -r requirements.txt
# This is the only file you need to deploy or embed the app.
streamlit>=1.58,<2
numpy>=2.4,<3
scipy>=1.17,<2
matplotlib>=3.11,<4
